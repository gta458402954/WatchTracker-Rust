# Codex：独立审查指令

Antigravity 已实施代码。你是最终验收者，不能直接相信执行者的自报结果。

读取 `.agent-work` 下的需求、恢复方案、恢复决策、分析、方案、验收标准、任务、执行日志和证据，同时检查实际代码差异、Git 状态、测试及构建配置。

如果正在审查 Recovery Phase：

1. 分别核对 `6fcbb1e` 和干净 `29ea3a4` 的 worktree、依赖、数据目录和命令证据。
2. 不允许把当前未提交差异、共享缓存或旧构建产物混入基线验证。
3. 应用双基线决策矩阵，必要时复核 bisect。
4. 只有 AC-R-001~005 全部有证据时才能将 AC-GATE-R 标记为 PASS 并开放 Phase A。

对所有 `IMPLEMENTED` 任务：

1. 改为 `REVIEWING`。
2. 审查需求符合性、边界条件、回归、安全、兼容性、无关修改、测试覆盖和硬编码规避行为。
3. 独立运行必要的构建、测试、类型检查、静态检查和人工验证。
4. 逐项核对验收标准，不得仅引用 Antigravity 日志作为通过依据。
5. 有问题时，在 `.agent-work/REVIEW_FEEDBACK.md` 写入唯一编号、严重程度、文件位置、复现、预期、实际、整改要求和验证方法，并将任务改为 `CHANGES_REQUESTED`。
6. 完全符合时才将任务改为 `ACCEPTED`。

此阶段若仍有未通过或未验证的必须项，不得生成声称通过的最终报告。
