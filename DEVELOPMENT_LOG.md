# Development Log

## 2026-05-10: 数据库引擎迁移 (sql.js -> node:sqlite)

### 变更背景
针对 WatchTracker 在便携版环境下启动加载慢（约 1 秒）的问题，利用 Node.js v22.5.0+ (Electron 36+) 内置的 `node:sqlite` 模块进行重构，并针对 I/O 和感知性能进行深度优化。

### 主要修改内容
1.  **主进程 (Main Process) 重构与异步化**:
    - 在 `electron/main.ts` 中引入 `node:sqlite` 的 `DatabaseSync`。
    - 将所有数据库初始化、Schema 维护、数据修复逻辑从渲染进程移至主进程。
    - **异步 I/O**: 将所有同步文件操作（`fs.mkdirSync`, `fs.existsSync` 等）替换为异步或更安全的处理流程，使用 `fs.promises` 避免阻塞主线程。
    - **窗口展示优化**: 使用 `win.once('ready-to-show')` 确保窗口在内容准备好后再显示，消除视觉闪烁。
2.  **预加载脚本 (Preload) 更新**:
    - 在 `electron/preload.ts` 中暴露了细粒度的数据库操作 API。
    - 删除了 `readDbFile` 和 `writeDbFile` 接口，提高了数据安全性与性能。
3.  **渲染进程 (Renderer) 感知优化**:
    - 重写 `src/utils/database.ts`，将其变为 IPC 代理，彻底移除 `sql.js`。
    - **骨架屏 (Skeleton Screen)**: 在 `App.tsx` 中引入 `loading` 状态和 CSS 骨架屏动画。数据加载期间显示占位 UI，实现“先渲染框架，后填充数据”，大幅提升感知流畅度。
4.  **工程化调整**:
    - `package.json`: 移除了 `sql.js` 和 `@types/sql.js` 依赖。
    - `vite.config.ts`: 删除了针对 `sql.js` 的构建排除项。
    - `update_categories.cjs`: 同步更新脚本，使用 `node:sqlite` 引擎。

### 收益
- **启动速度**: 数据库就绪时间从 ~1s 降低到 **近乎瞬时**，配合异步 I/O 消除主线程卡顿。
- **感知性能**: 骨架屏让用户在数据加载期间有明确的视觉反馈，消除“白屏”等待感。
- **体积与稳定性**: 移除了 WASM 依赖，包体积更小，内存占用更低，运行更稳定。

---

## 2026-05-16: 极致启动并行化与凭据恢复优化

### 主要修改内容
1.  **启动并行化**: 
    - 主进程 `backupDatabase` 和 `initDatabase` 改为异步后台执行，不阻塞 `createWindow`。
    - 窗口显示逻辑改为 `show: true` 配合 `backgroundColor: '#f9fafb'`，实现点击即开窗的视觉体验。
2.  **渲染进程加速**: 
    - 移除 `main.tsx` 中的 `await initDatabase()`，应用完全同步挂载，配合骨架屏实现秒开。
3.  **凭据解密自动恢复**: 
    - 在 `SettingsModal.tsx` 中增加解密错误捕获。
    - 当检测到 Windows 安全存储密钥失效（跨设备移动时）时，自动清除旧凭据并提示用户重填，解决同步静默失败问题。

---

## 后续优化路线图 (Roadmap)

### 1. 数据库性能与维护
-   **定期 VACUUM**: 在设置中增加“压缩数据库”功能，释放物理删除后的磁盘空间。
-   **搜索索引**: 随着记录增加，在 `chineseName` 和 `originalName` 上创建 `NOCASE` 索引，提升万级数据下的搜索速度。

### 2. UI/UX 深度体验
-   **长列表虚拟化**: 引入 `react-window`，针对超过 1000 条记录的情况，只渲染可视区域卡片，解决滚动卡顿。
-   **海报资源优化**: 
    - 下载后自动转为 WebP 格式。
    - 增加缩略图生成逻辑，海报墙展示小图，详情页展示原图，减小内存占用。
-   **同步状态细化**: 在主界面 StatsBar 增加微小的同步进度指示器（如：正在上传、最后同步时间）。

### 3. 开发与部署自动化
-   **一键生产部署**: 修改 `build-all.js`，增加 `--prod` 参数，支持编译后自动同步到 `D:\Program File\WatchTracker-portable-new`。
-   **ASAR 瘦身**: 检查生产包体积，排除 `node_modules` 中不必要的开发依赖及 `.map` 文件。

---

## 2026-05-17: Rust 版本便携化重构与功能深度增强

### 主要修改内容
1.  **真正的便携化支持 (Portable Mode)**:
    - **路径重定向**: 修改 Rust 后端 (`db.rs`, `net.rs`, `lib.rs`)，使程序启动时优先检测运行目录下是否存在 `data/` 文件夹。
    - **独立存储**: 数据库 (`watchtracker.db`) 和海报文件夹 (`posters/`) 现已完全脱离系统 `AppData`，支持在 U 盘中绿色运行。
2.  **数据库性能与维护优化**:
    - **定期 VACUUM**: 在“设置 -> 高级维护”中增加“压缩数据库”功能，通过 SQLite `VACUUM` 指令整理磁盘空间。
    - **搜索索引加速**: 数据库版本升级至 `v3`，为 `chineseName` 和 `originalName` 字段创建 `INDEX`，大幅提升模糊搜索速度。
3.  **UI/UX 体验细化**:
    - **同步状态可视化**: 在顶部 `StatsBar` 增加同步进度动画及“上次同步时间”显示，提供直观的数据安全反馈。
    - **自动同步频率可调**: 在“设置”中新增“自动同步设置”，允许用户通过滑动条自定义同步触发的延迟时间（5秒至300秒）。
    - **TMDB 搜索增强**: 
        - 错误信息红色高亮显示。
        - 针对网络环境自动简化错误提示（如“网络连接失败”），提升用户友好度。
    - **描述优化**: 将主界面同步按钮的说明更新为“手动同步到坚果云”，消除歧义。
5.  **致命 Bug 修复**:
    - **TMDB 搜索崩溃修复**: 修复了由于错误处理 TMDB API 响应对象（将其误认为数组）导致的 `p.map is not a function` 崩溃问题。现在已正确提取 `results` 字段。
4.  **工程化整理**:
    - 自动化整理便携版目录 `WatchTracker-Rust-Portable`，包含主程序、数据目录及详细使用说明文档。

### 收益
- **零配置移动**: 用户只需拷贝整个文件夹即可在任意设备上使用，无需重新配置环境。
- **性能更强**: 索引优化让搜索近乎无延迟，VACUUM 功能确保长期使用下的磁盘整洁。
- **更强的交互感**: 错误提示和同步状态让用户对应用运行状况有清晰的感知。

---
*Generated by Gemini CLI*
